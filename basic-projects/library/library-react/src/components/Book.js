import React from "react";
import PropTypes from "prop-types";

const Book = (props) => {
  return <div className="book-container"></div>;
};

Book.propTypes = {
  book: PropTypes.object,
};

export default Book;
